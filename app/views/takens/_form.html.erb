<%= form_for(taken) do |f| %>
  <% if taken.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(taken.errors.count, "error") %> prohibited this taken from being saved:</h2>

      <ul>
      <% taken.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="form-group">
  <div class="field">
    <%= f.label :student_id %>
    <%= f.text_field :student_id , class: "form-control"%>
  </div>

  <div class="field">
    <%= f.label :course_id %>
    <%= f.text_field :course_id , class: "form-control"%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
</div>
<% end %>
